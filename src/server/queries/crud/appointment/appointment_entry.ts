import { Appointment } from "~/server/db_schema/type_def";

/*
 export async function create_appointment_entry(
    {customer_id, date, time, duration, details}: {
        customer_id: string,
        date: string,
        time: string,
        duration: string,
        details: string,
    }, 
    redirect: string): Promise<Appointment> {
 }
 export async function retrieve_appointment_entry(redirect: string): Promise<Appointment | null> {
 }
 export async function update_appointment_entry(redirect: string) {
 }
 export async function delete_appointment_entry(redirect: string) {
 }
*/
